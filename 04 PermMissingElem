class Solution {
    public int solution(int[] A) {
        // write your code in Java SE 8
        int size = A.length +1;
        long sum = (size * (size+1)) / 2;
        
        for (int number : A){
            sum -= number;    
        }
        
        return (int)sum;
    }
}
